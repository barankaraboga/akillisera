<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barankaraboa.akilisera" >

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/icondraw"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Cicekler"/>
        <activity android:name=".icerikler.elmaVeri"/>
        <activity android:name=".icerikler.kirazVeri"/>
        <activity android:name=".icerikler.portakalVeri"/>
        <activity android:name=".Agaclar"/>

        <activity android:name=".icerikler.gul"/>
        <activity android:name=".icerikler.lale"/>
        <activity android:name=".icerikler.zambak"/>
<activity android:name=".hakkimizda"/>

    </application>

</manifest>